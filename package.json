{
  "name": "graf",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-addon": "cd addon && node-gyp configure && node-gyp build",
    "rebuild": "electron-rebuild -f -w calculations",
    "start": "npm run build-addon && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "electron": "^41.3.0",
    "node-addon-api": "^8.7.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "electron-rebuild": "^3.2.9",
    "node-gyp": "^12.3.0"
  }
}
